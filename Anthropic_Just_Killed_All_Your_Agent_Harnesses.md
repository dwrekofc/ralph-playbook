# Anthropic Just Killed All Your Agent Harnesses

**Source:** https://www.youtube.com/watch?v=nBH07G-zayk

Over the past few months, we have Over the past few months, we have covered many AI coding frameworks covered many AI coding frameworks covered many AI coding frameworks including BMAD, GSD, SpecKit, and including BMAD, GSD, SpecKit, and including BMAD, GSD, SpecKit, and Superpowers. And a lot of you actually Superpowers. And a lot of you actually Superpowers. And a lot of you actually started using them.

But Anthropic just started using them. But Anthropic just started using them. But Anthropic just ran experiments on their own harness, ran experiments on their own harness, ran experiments on their own harness, removing components one by one and removing components one by one and removing components one by one and measuring what actually mattered. Their measuring what actually mattered.

Their measuring what actually mattered. Their conclusion was that most of it is now a conclusion was that most of it is now a conclusion was that most of it is now a dead weight. Every component in a dead weight. Every component in a dead weight.

Every component in a framework encodes an assumption about framework encodes an assumption about framework encodes an assumption about what the model cannot do on its own. And what the model cannot do on its own. And what the model cannot do on its own. And with Opus 4.6, those assumptions have with Opus 4.6, those assumptions have with Opus 4.6, those assumptions have gone stale.

We went through the whole gone stale. We went through the whole gone stale. We went through the whole thing and mapped out what still matters, thing and mapped out what still matters, thing and mapped out what still matters, what you can strip out, and what your what you can strip out, and what your what you can strip out, and what your setup should actually look like. Now, setup should actually look like.

Now, setup should actually look like. Now, agent harnesses play an important role agent harnesses play an important role agent harnesses play an important role in making agents work substantially in making agents work substantially in making agents work substantially better over long horizons. Anthropic has better over long horizons. Anthropic has better over long horizons.

Anthropic has already released an agent harness, which already released an agent harness, which already released an agent harness, which we covered in detail in a previous video we covered in detail in a previous video we covered in detail in a previous video explaining how to set it up and use it. explaining how to set it up and use it. explaining how to set it up and use it. We have also covered other frameworks in We have also covered other frameworks in We have also covered other frameworks in that same context, and while their that same context, and while their that same context, and while their implementations differ, they are all implementations differ, they are all implementations differ, they are all trying to do the same thing.

But when trying to do the same thing. But when trying to do the same thing. But when these frameworks were released, the these frameworks were released, the these frameworks were released, the models were not as capable as Opus 4.6 models were not as capable as Opus 4.6 models were not as capable as Opus 4.6 is now. For example, frameworks like GST is now.

For example, frameworks like GST is now. For example, frameworks like GST are focused on context isolation, but are focused on context isolation, but are focused on context isolation, but that is not a problem with Opus 4.6, not that is not a problem with Opus 4.6, not that is not a problem with Opus 4.6, not only because of the million token only because of the million token only because of the million token context window, but another reason that context window, but another reason that context window, but another reason that we will talk about in a bit. Therefore, we will talk about in a bit. Therefore, we will talk about in a bit.

Therefore, a lot of previously implemented a lot of previously implemented a lot of previously implemented frameworks are now an overhead for new frameworks are now an overhead for new frameworks are now an overhead for new model capabilities. Anthropic actually model capabilities. Anthropic actually model capabilities. Anthropic actually ran experiments testing out different ran experiments testing out different ran experiments testing out different aspects of the harness, removing each aspects of the harness, removing each aspects of the harness, removing each one and measuring its impact.

From their one and measuring its impact. From their one and measuring its impact. From their findings, they concluded that all an findings, they concluded that all an findings, they concluded that all an agent harness actually needs is agents agent harness actually needs is agents agent harness actually needs is agents for planning, generation, and for planning, generation, and for planning, generation, and evaluation. The rest are just ways of evaluation.

The rest are just ways of evaluation. The rest are just ways of doing things that become dead weight doing things that become dead weight doing things that become dead weight given how capable the models are. Now given how capable the models are. Now given how capable the models are.

Now the core theory is that every component the core theory is that every component the core theory is that every component in an agent harness, no matter which one in an agent harness, no matter which one in an agent harness, no matter which one you are using, relies on the same you are using, relies on the same you are using, relies on the same principle. Each component encodes an principle. Each component encodes an principle. Each component encodes an assumption about what the model can do assumption about what the model can do assumption about what the model can do on its own.

These assumptions should be on its own. These assumptions should be on its own. These assumptions should be stress tested because they may be stress tested because they may be stress tested because they may be incorrect and they will go stale as the incorrect and they will go stale as the incorrect and they will go stale as the model improves and that's what they did model improves and that's what they did model improves and that's what they did throughout the article. Therefore, with throughout the article.

Therefore, with throughout the article. Therefore, with the evolution of the models, your the evolution of the models, your the evolution of the models, your harness should also evolve. And if you harness should also evolve. And if you harness should also evolve.

And if you are working with the same principles are working with the same principles are working with the same principles laid out a few months back, you are not laid out a few months back, you are not laid out a few months back, you are not keeping up. Planning is the first step keeping up. Planning is the first step keeping up. Planning is the first step that remains unchanged across every that remains unchanged across every that remains unchanged across every framework.

But the way you plan has to framework. But the way you plan has to framework. But the way you plan has to change for more capable models. change for more capable models.

change for more capable models. Anthropic's previous long-running Anthropic's previous long-running Anthropic's previous long-running harnesses required the user to provide a harnesses required the user to provide a harnesses required the user to provide a detailed spec upfront. Frameworks like detailed spec upfront. Frameworks like detailed spec upfront.

Frameworks like BMAD and specit literally shard the task BMAD and specit literally shard the task BMAD and specit literally shard the task into smaller fragments and microtasks into smaller fragments and microtasks into smaller fragments and microtasks that help the AI agent implement it with that help the AI agent implement it with that help the AI agent implement it with ease. And these weren't just small ease. And these weren't just small ease. And these weren't just small tasks.

They were literally detailed tasks. They were literally detailed tasks. They were literally detailed steps that agents just had to follow steps that agents just had to follow steps that agents just had to follow without thinking. This is because at without thinking.

This is because at without thinking. This is because at that time the models were not capable that time the models were not capable that time the models were not capable enough and needed to be microg guided so enough and needed to be microg guided so enough and needed to be microg guided so that they could perform the way you that they could perform the way you that they could perform the way you wanted. But with Opus 4.5 and 4.6 this wanted. But with Opus 4.5 and 4.6 this wanted.

But with Opus 4.5 and 4.6 this has changed. When Anthropic tested this, has changed. When Anthropic tested this, has changed. When Anthropic tested this, they found that if the planner tried to they found that if the planner tried to they found that if the planner tried to specify micro technical details up specify micro technical details up specify micro technical details up front, a single error would cascade front, a single error would cascade front, a single error would cascade through every level of implementation, through every level of implementation, through every level of implementation, making it hard for the agent to deviate making it hard for the agent to deviate making it hard for the agent to deviate and fix issues on its own.

It all relied and fix issues on its own. It all relied and fix issues on its own. It all relied on how well written the plan was. on how well written the plan was.

on how well written the plan was. Therefore, planning has now become high Therefore, planning has now become high Therefore, planning has now become high level rather than a detailed technical level rather than a detailed technical level rather than a detailed technical implementation. Agents are much smarter implementation. Agents are much smarter implementation.

Agents are much smarter on their own now and you just have to on their own now and you just have to on their own now and you just have to tell them what deliverables are needed. tell them what deliverables are needed. tell them what deliverables are needed. They can figure out the path toward that They can figure out the path toward that They can figure out the path toward that on their own.

With this shift, planning on their own. With this shift, planning on their own. With this shift, planning approaches like those in BMAD and specit approaches like those in BMAD and specit approaches like those in BMAD and specit are no longer as relevant. You can limit are no longer as relevant.

You can limit are no longer as relevant. You can limit BMAD to the planning phase up to PRD BMAD to the planning phase up to PRD BMAD to the planning phase up to PRD generation with no need to go into the generation with no need to go into the generation with no need to go into the technical sharding process. As we have technical sharding process. As we have technical sharding process.

As we have mentioned before, PRD generation with mentioned before, PRD generation with mentioned before, PRD generation with BMAD is effective because it has BMAD is effective because it has BMAD is effective because it has specialized agents for understanding specialized agents for understanding specialized agents for understanding product requirements better than Claude product requirements better than Claude product requirements better than Claude would have done on its own. This is would have done on its own. This is would have done on its own. This is because those agents have the external because those agents have the external because those agents have the external context for specific tasks added in by context for specific tasks added in by context for specific tasks added in by the author.

Alternatively, you can use the author. Alternatively, you can use the author. Alternatively, you can use the questioning session from superpowers the questioning session from superpowers the questioning session from superpowers since it was actually intended to since it was actually intended to since it was actually intended to identify edge cases which can be more identify edge cases which can be more identify edge cases which can be more effective than multi-level task effective than multi-level task effective than multi-level task documentation. But the core problem with documentation.

But the core problem with documentation. But the core problem with overly detailed planning is that it overly detailed planning is that it overly detailed planning is that it locks the agent down and does not leave locks the agent down and does not leave locks the agent down and does not leave room for the AI to make discoveries and room for the AI to make discoveries and room for the AI to make discoveries and figure things out on its own. Anthropic figure things out on its own. Anthropic figure things out on its own.

Anthropic has also given an example plan that was has also given an example plan that was has also given an example plan that was generated by the planner agent which you generated by the planner agent which you generated by the planner agent which you can use to set up your own planner can use to set up your own planner can use to set up your own planner agent. It clearly outlines that the plan agent. It clearly outlines that the plan agent. It clearly outlines that the plan should go big on scope and push the should go big on scope and push the should go big on scope and push the boundaries of whatever app idea you boundaries of whatever app idea you boundaries of whatever app idea you provide.

The core idea is to keep the provide. The core idea is to keep the provide. The core idea is to keep the project at the product level, not the project at the product level, not the project at the product level, not the implementation level. This matters implementation level.

This matters implementation level. This matters because if it tries to plan out the because if it tries to plan out the because if it tries to plan out the implementation within the project plan, implementation within the project plan, implementation within the project plan, it becomes too focused on technical it becomes too focused on technical it becomes too focused on technical details and may fail to deliver what is details and may fail to deliver what is details and may fail to deliver what is actually needed for a complete product. actually needed for a complete product. actually needed for a complete product.

Now, you might think that Claude's own Now, you might think that Claude's own Now, you might think that Claude's own plan mode already does similar planning plan mode already does similar planning plan mode already does similar planning by asking questions and providing a by asking questions and providing a by asking questions and providing a detailed plan. But here is the detailed plan. But here is the detailed plan. But here is the difference.

Even though Claude has a difference. Even though Claude has a difference. Even though Claude has a planning agent, it still focuses heavily planning agent, it still focuses heavily planning agent, it still focuses heavily on implementation details and does not on implementation details and does not on implementation details and does not truly operate at the product level, truly operate at the product level, truly operate at the product level, which goes against Anthropic's findings. which goes against Anthropic's findings.

which goes against Anthropic's findings. Therefore, once you have this in place, Therefore, once you have this in place, Therefore, once you have this in place, you can simply ask Claude to use the you can simply ask Claude to use the you can simply ask Claude to use the agent you created to plan your app, and agent you created to plan your app, and agent you created to plan your app, and it will generate a complete plan and it will generate a complete plan and it will generate a complete plan and document it in your folder as it document it in your folder as it document it in your folder as it progresses. This plan includes a full progresses. This plan includes a full progresses.

This plan includes a full feature breakdown at the product level, feature breakdown at the product level, feature breakdown at the product level, and with each phase, it includes user and with each phase, it includes user and with each phase, it includes user stories that show what the user's stories that show what the user's stories that show what the user's perspective looks like. This helps perspective looks like. This helps perspective looks like. This helps Claude implement the correct workflows Claude implement the correct workflows Claude implement the correct workflows that users actually expect.

But before that users actually expect. But before that users actually expect. But before we move forwards, let's have a word by we move forwards, let's have a word by we move forwards, let's have a word by our sponsor, Miniax. Setting up AI our sponsor, Miniax.

Setting up AI our sponsor, Miniax. Setting up AI agents is a nightmare. API keys, server agents is a nightmare. API keys, server agents is a nightmare.

API keys, server configs, Docker setups, and after all configs, Docker setups, and after all configs, Docker setups, and after all that, your assistant forgets everything that, your assistant forgets everything that, your assistant forgets everything the moment you close the tab. The the moment you close the tab. The the moment you close the tab. The solution is Maxclaw, a cloudpowered AI solution is Maxclaw, a cloudpowered AI solution is Maxclaw, a cloudpowered AI at your fingertips.

No setup, no at your fingertips. No setup, no at your fingertips. No setup, no headaches. You can deploy your own Open headaches.

You can deploy your own Open headaches. You can deploy your own Open Claw. Just click deploy, and you're live Claw. Just click deploy, and you're live Claw.

Just click deploy, and you're live in under 10 seconds. It builds websites, in under 10 seconds. It builds websites, in under 10 seconds. It builds websites, writes code, runs research, and writes code, runs research, and writes code, runs research, and automates your busy work, all from automates your busy work, all from automates your busy work, all from simple text prompts.

MaxClaw connects simple text prompts. MaxClaw connects simple text prompts. MaxClaw connects directly to Telegram, Slack, Discord, directly to Telegram, Slack, Discord, directly to Telegram, Slack, Discord, and more, letting you automate and more, letting you automate and more, letting you automate workflows, browse the web, and even workflows, browse the web, and even workflows, browse the web, and even generate images or videos, all from a generate images or videos, all from a generate images or videos, all from a simple chat. It is part of Miniax agent, simple chat.

It is part of Miniax agent, simple chat. It is part of Miniax agent, an AI native workspace where everyone an AI native workspace where everyone an AI native workspace where everyone becomes an agent designer. It works on becomes an agent designer. It works on becomes an agent designer.

It works on Mac, Windows, powered by M2.7, which Mac, Windows, powered by M2.7, which Mac, Windows, powered by M2.7, which matches Claude Opus 4.6 on Sweetbench. matches Claude Opus 4.6 on Sweetbench. matches Claude Opus 4.6 on Sweetbench. Stop wrestling with complex setups.

Let Stop wrestling with complex setups. Let Stop wrestling with complex setups. Let Maxclaw handle it, and click the link in Maxclaw handle it, and click the link in Maxclaw handle it, and click the link in the pinned comment to get started. The the pinned comment to get started.

The the pinned comment to get started. The agent that writes the code should not be agent that writes the code should not be agent that writes the code should not be the one evaluating it. This is the the one evaluating it. This is the the one evaluating it.

This is the second most common problem and it is not second most common problem and it is not second most common problem and it is not usually discussed much. Self-evaluation usually discussed much. Self-evaluation usually discussed much. Self-evaluation is problematic because if you use the is problematic because if you use the is problematic because if you use the same agent that wrote the code to same agent that wrote the code to same agent that wrote the code to evaluate it, it tends to respond very evaluate it, it tends to respond very evaluate it, it tends to respond very confidently and praise its own work even confidently and praise its own work even confidently and praise its own work even when the quality is clearly subpar.

This when the quality is clearly subpar. This when the quality is clearly subpar. This might be easier to manage for tasks that might be easier to manage for tasks that might be easier to manage for tasks that have quantitative metrics like whether have quantitative metrics like whether have quantitative metrics like whether the APIs that were implemented are the APIs that were implemented are the APIs that were implemented are actually working. But this problem actually working.

But this problem actually working. But this problem becomes much more pronounced for tasks becomes much more pronounced for tasks becomes much more pronounced for tasks that do not have clearly verifiable that do not have clearly verifiable that do not have clearly verifiable outcomes. The biggest example of this is outcomes. The biggest example of this is outcomes.

The biggest example of this is the UI. What constitutes a good UI is the UI. What constitutes a good UI is the UI. What constitutes a good UI is subjective and AI might not fully grasp subjective and AI might not fully grasp subjective and AI might not fully grasp your intentions.

It may consider its own your intentions. It may consider its own your intentions. It may consider its own implementation as well done even if it implementation as well done even if it implementation as well done even if it does not meet your standards. This issue does not meet your standards.

This issue does not meet your standards. This issue was already recognized by the creators was already recognized by the creators was already recognized by the creators of multiple frameworks and they of multiple frameworks and they of multiple frameworks and they implemented their own evaluation implemented their own evaluation implemented their own evaluation mechanisms to address it. All of the mechanisms to address it. All of the mechanisms to address it.

All of the frameworks we have covered like GSD, frameworks we have covered like GSD, frameworks we have covered like GSD, BMAD and superpowers ensure that the BMAD and superpowers ensure that the BMAD and superpowers ensure that the same agent that wrote the code does not same agent that wrote the code does not same agent that wrote the code does not get to evaluate its quality. This get to evaluate its quality. This get to evaluate its quality. This approach significantly improves the approach significantly improves the approach significantly improves the accuracy and reliability of the agents accuracy and reliability of the agents accuracy and reliability of the agents evaluations.

Therefore, whether you are evaluations. Therefore, whether you are evaluations. Therefore, whether you are using an existing framework or building using an existing framework or building using an existing framework or building on your own, you need to ensure that the on your own, you need to ensure that the on your own, you need to ensure that the evaluator is completely separate from evaluator is completely separate from evaluator is completely separate from the implement. Before implementation the implement.

Before implementation the implement. Before implementation begins, both the generator and evaluator begins, both the generator and evaluator begins, both the generator and evaluator agents negotiate a contract agreeing on agents negotiate a contract agreeing on agents negotiate a contract agreeing on what done looks like for the work. This what done looks like for the work. This what done looks like for the work.

This helps because both agents clearly know helps because both agents clearly know helps because both agents clearly know what to achieve and what to verify. With what to achieve and what to verify. With what to achieve and what to verify. With highle planning, there still needs to be highle planning, there still needs to be highle planning, there still needs to be actionable implementable steps.

But actionable implementable steps. But actionable implementable steps. But during testing with the harness, they during testing with the harness, they during testing with the harness, they tried removing the sprint contract. They tried removing the sprint contract.

They tried removing the sprint contract. They found that Opus 4.5 was less efficient found that Opus 4.5 was less efficient found that Opus 4.5 was less efficient in this scenario because the evaluator in this scenario because the evaluator in this scenario because the evaluator still had to step in to catch issues. still had to step in to catch issues. still had to step in to catch issues.

But with Opus 4.6, the model's But with Opus 4.6, the model's But with Opus 4.6, the model's capabilities had improved so much that capabilities had improved so much that capabilities had improved so much that the contract was not necessary. The the contract was not necessary. The the contract was not necessary. The generative agent was capable enough to generative agent was capable enough to generative agent was capable enough to handle most of the work on its own.

handle most of the work on its own. handle most of the work on its own. Therefore, for smaller models like Therefore, for smaller models like Therefore, for smaller models like Sonnet or Haiku, you still need to Sonnet or Haiku, you still need to Sonnet or Haiku, you still need to document tasks, break them down properly document tasks, break them down properly document tasks, break them down properly into sprint structures and have each into sprint structures and have each into sprint structures and have each agent agree on what complete looks like. agent agree on what complete looks like.

agent agree on what complete looks like. But with more capable models, you can But with more capable models, you can But with more capable models, you can rely on Opus to execute the highle plan rely on Opus to execute the highle plan rely on Opus to execute the highle plan without these additional steps. Now, we without these additional steps. Now, we without these additional steps.

Now, we said that there is a reason why context said that there is a reason why context said that there is a reason why context isolation matters. This is because isolation matters. This is because isolation matters. This is because smaller models experience context smaller models experience context smaller models experience context anxiety, a phenomenon where models start anxiety, a phenomenon where models start anxiety, a phenomenon where models start losing coherence on lengthy tasks as losing coherence on lengthy tasks as losing coherence on lengthy tasks as their context window fills up.

When this their context window fills up. When this their context window fills up. When this happens, they wrap up work prematurely happens, they wrap up work prematurely happens, they wrap up work prematurely and claim they have implemented tasks and claim they have implemented tasks and claim they have implemented tasks correctly, even when they have not. The correctly, even when they have not.

The correctly, even when they have not. The solution that helped was a context solution that helped was a context solution that helped was a context reset, clearing their context windows reset, clearing their context windows reset, clearing their context windows before starting implementation. Since before starting implementation. Since before starting implementation.

Since the context was cleared, they could rely the context was cleared, they could rely the context was cleared, they could rely on a task breakdown documented on a task breakdown documented on a task breakdown documented externally, which persisted across externally, which persisted across externally, which persisted across context resets. But the models exhibited context resets. But the models exhibited context resets. But the models exhibited so much context anxiety that compaction so much context anxiety that compaction so much context anxiety that compaction alone was not enough.

They needed alone was not enough. They needed alone was not enough. They needed additional measures to prevent problems additional measures to prevent problems additional measures to prevent problems on longer tasks. Starting with Opus 4.5.

on longer tasks. Starting with Opus 4.5. on longer tasks. Starting with Opus 4.5.

However, models no longer exhibit this However, models no longer exhibit this However, models no longer exhibit this behavior. These agents can run behavior. These agents can run behavior. These agents can run continuously across an entire session continuously across an entire session continuously across an entire session and the way Claude handles compaction is and the way Claude handles compaction is and the way Claude handles compaction is sufficient for their functioning.

sufficient for their functioning. sufficient for their functioning. Therefore, context resets are no longer Therefore, context resets are no longer Therefore, context resets are no longer necessary and detailed task breakdowns necessary and detailed task breakdowns necessary and detailed task breakdowns like those in BMAD and spec kit are not like those in BMAD and spec kit are not like those in BMAD and spec kit are not needed either with just highle guidance needed either with just highle guidance needed either with just highle guidance alone being enough. The generator agent alone being enough.

The generator agent alone being enough. The generator agent is the main implement that actually is the main implement that actually is the main implement that actually builds the app feature by feature. It builds the app feature by feature. It builds the app feature by feature.

It takes the specs from the plan and takes the specs from the plan and takes the specs from the plan and continuously implements them while continuously implements them while continuously implements them while integrating with Git for version integrating with Git for version integrating with Git for version control. The generator works in control. The generator works in control. The generator works in coordination with the evaluator agent.

coordination with the evaluator agent. coordination with the evaluator agent. After building a feature, it hands it After building a feature, it hands it After building a feature, it hands it over for testing and receives feedback over for testing and receives feedback over for testing and receives feedback to improve its implementation. Its to improve its implementation.

Its to improve its implementation. Its workflow is organized into several workflow is organized into several workflow is organized into several steps. Understanding the task, steps. Understanding the task, steps.

Understanding the task, implementing it, and refining the implementing it, and refining the implementing it, and refining the implementation. Even within the implementation. Even within the implementation. Even within the implementation phase, work is divided implementation phase, work is divided implementation phase, work is divided into four subphases covering different into four subphases covering different into four subphases covering different aspects.

It follows the design aspects. It follows the design aspects. It follows the design direction, verifies its work, and then direction, verifies its work, and then direction, verifies its work, and then hands it to the evaluator. This creates hands it to the evaluator.

This creates hands it to the evaluator. This creates a structured step-by-step pattern a structured step-by-step pattern a structured step-by-step pattern enabling the agent to implement an enabling the agent to implement an enabling the agent to implement an entire app independently and entire app independently and entire app independently and systematically. The evaluator agent acts systematically. The evaluator agent acts systematically.

The evaluator agent acts as the adversary to the generator. Its as the adversary to the generator. Its as the adversary to the generator. Its job is to ensure the app is implemented job is to ensure the app is implemented job is to ensure the app is implemented correctly, not by doing a generic find correctly, not by doing a generic find correctly, not by doing a generic find bugs pass, but by approaching it bugs pass, but by approaching it bugs pass, but by approaching it critically from the perspective that critically from the perspective that critically from the perspective that bugs exist.

It can use tools like bugs exist. It can use tools like bugs exist. It can use tools like playright to test the app by simulating playright to test the app by simulating playright to test the app by simulating user interactions, identify bugs based user interactions, identify bugs based user interactions, identify bugs based on predefined criteria, and send on predefined criteria, and send on predefined criteria, and send feedback back to the generator. By feedback back to the generator.

By feedback back to the generator. By reading the plan, the evaluator gains a reading the plan, the evaluator gains a reading the plan, the evaluator gains a clear understanding of what done should clear understanding of what done should clear understanding of what done should look like and checks everything look like and checks everything look like and checks everything thoroughly before approving it. Each thoroughly before approving it. Each thoroughly before approving it.

Each framework has its own validator, but the framework has its own validator, but the framework has its own validator, but the approaches differ significantly. BMAD approaches differ significantly. BMAD approaches differ significantly. BMAD uses specialized code review and QA uses specialized code review and QA uses specialized code review and QA agents that generate and run tests, agents that generate and run tests, agents that generate and run tests, evaluating the code from multiple evaluating the code from multiple evaluating the code from multiple angles.

GSD uses a verifier sub agent angles. GSD uses a verifier sub agent angles. GSD uses a verifier sub agent that checks the implementation against that checks the implementation against that checks the implementation against the existing plan and produces a the existing plan and produces a the existing plan and produces a documentation report. Superpowers relies documentation report.

Superpowers relies documentation report. Superpowers relies on fresh sub aents and enforces strict on fresh sub aents and enforces strict on fresh sub aents and enforces strict TDD where no code can be written before TDD where no code can be written before TDD where no code can be written before the test cases. If the agent tries to the test cases. If the agent tries to the test cases.

If the agent tries to bypass this, it is blocked. Specit bypass this, it is blocked. Specit bypass this, it is blocked. Specit treats specs as the source of truth and treats specs as the source of truth and treats specs as the source of truth and allows the agent to verify code against allows the agent to verify code against allows the agent to verify code against the documentation.

But none of these the documentation. But none of these the documentation. But none of these frameworks provide a scoring mechanism frameworks provide a scoring mechanism frameworks provide a scoring mechanism with the level of rigor Anthropic was with the level of rigor Anthropic was with the level of rigor Anthropic was aiming for. Therefore, the evaluator in aiming for.

Therefore, the evaluator in aiming for. Therefore, the evaluator in anthropics harness is the closest to anthropics harness is the closest to anthropics harness is the closest to Ralph Loop's strict implementation Ralph Loop's strict implementation Ralph Loop's strict implementation enforcement for Claude, ensuring the enforcement for Claude, ensuring the enforcement for Claude, ensuring the agent actually delivers what is needed agent actually delivers what is needed agent actually delivers what is needed with a proper graded evaluation with a proper graded evaluation with a proper graded evaluation mechanism. Also, if you are enjoying our mechanism. Also, if you are enjoying our mechanism.

Also, if you are enjoying our content, consider pressing the hype content, consider pressing the hype content, consider pressing the hype button because it helps us create more button because it helps us create more button because it helps us create more content like this and reach out to more content like this and reach out to more content like this and reach out to more people. The agent has no means to know people. The agent has no means to know people. The agent has no means to know what the right output looks like for what the right output looks like for what the right output looks like for you, especially in cases where the you, especially in cases where the you, especially in cases where the implementation is not quantifiable.

implementation is not quantifiable. implementation is not quantifiable. Therefore, you use graded evaluation Therefore, you use graded evaluation Therefore, you use graded evaluation mechanisms so that they know what the mechanisms so that they know what the mechanisms so that they know what the right output looks like to you. When right output looks like to you.

When right output looks like to you. When Anthropic gave an example for the Anthropic gave an example for the Anthropic gave an example for the evaluation metrics for the front end, evaluation metrics for the front end, evaluation metrics for the front end, they mentioned that the AI tends to they mentioned that the AI tends to they mentioned that the AI tends to converge on similar outputs most of the converge on similar outputs most of the converge on similar outputs most of the time. They set four grading criteria for time. They set four grading criteria for time.

They set four grading criteria for both the generator and evaluator agents. both the generator and evaluator agents. both the generator and evaluator agents. The first is quality of the design, The first is quality of the design, The first is quality of the design, instructing it to check if the field is instructing it to check if the field is instructing it to check if the field is coherent or just separate components coherent or just separate components coherent or just separate components strung together.

Then originality, which strung together. Then originality, which strung together. Then originality, which is one of the main ones because AI tends is one of the main ones because AI tends is one of the main ones because AI tends to default to the same purple and white to default to the same purple and white to default to the same purple and white gradient pattern for most UIs. This goes gradient pattern for most UIs.

This goes gradient pattern for most UIs. This goes against how humans design because for a against how humans design because for a against how humans design because for a human, each design choice is deliberate human, each design choice is deliberate human, each design choice is deliberate and this makes it easily identifiable and this makes it easily identifiable and this makes it easily identifiable when the website does not look good. The when the website does not look good. The when the website does not look good.

The third is craft. The minor details like third is craft. The minor details like third is craft. The minor details like typography, spacing, consistency, and typography, spacing, consistency, and typography, spacing, consistency, and color harmony where the contrast ratio color harmony where the contrast ratio color harmony where the contrast ratio is technically balanced rather than is technically balanced rather than is technically balanced rather than giving it a more creative look.

And the giving it a more creative look. And the giving it a more creative look. And the last is functionality. Because in terms last is functionality.

Because in terms last is functionality. Because in terms of UI, each component plays a visual of UI, each component plays a visual of UI, each component plays a visual role in enhancing the user experience. role in enhancing the user experience. role in enhancing the user experience.

Claude already scores well on craft and Claude already scores well on craft and Claude already scores well on craft and functionality, but the rest are the most functionality, but the rest are the most functionality, but the rest are the most common struggles and the prompts need to common struggles and the prompts need to common struggles and the prompts need to push it to its best capability by push it to its best capability by push it to its best capability by emphasizing that the best design comes emphasizing that the best design comes emphasizing that the best design comes from quality. Therefore, when you are from quality. Therefore, when you are from quality. Therefore, when you are building your app, you can set up building your app, you can set up building your app, you can set up similar criteria for as many features as similar criteria for as many features as similar criteria for as many features as you want like code architecture, the you want like code architecture, the you want like code architecture, the front end, UX, user flows, and more.

front end, UX, user flows, and more. front end, UX, user flows, and more. have each part mentioned in the criteria have each part mentioned in the criteria have each part mentioned in the criteria have a dedicated score so that the model have a dedicated score so that the model have a dedicated score so that the model can identify its importance based on how can identify its importance based on how can identify its importance based on how well it performs. These files are well it performs.

These files are well it performs. These files are referenced in the evaluator agent referenced in the evaluator agent referenced in the evaluator agent because the evaluator's job is to score because the evaluator's job is to score because the evaluator's job is to score so it knows what rubric it should be so it knows what rubric it should be so it knows what rubric it should be following. Given everything we have following. Given everything we have following.

Given everything we have covered, you might wonder what you covered, you might wonder what you covered, you might wonder what you should actually do now. If you want a should actually do now. If you want a should actually do now. If you want a framework so that your setup is easier, framework so that your setup is easier, framework so that your setup is easier, go for GSD because GSD inherently uses go for GSD because GSD inherently uses go for GSD because GSD inherently uses the planner generator evaluator loop by the planner generator evaluator loop by the planner generator evaluator loop by default.

But its evaluator just matches default. But its evaluator just matches default. But its evaluator just matches the code against the existing plans and the code against the existing plans and the code against the existing plans and relies on user acceptance testing. It relies on user acceptance testing.

It relies on user acceptance testing. It uses a pass and fail mechanism, not a uses a pass and fail mechanism, not a uses a pass and fail mechanism, not a scored implementation. Therefore, you scored implementation. Therefore, you scored implementation.

Therefore, you can take the best parts of the anthropic can take the best parts of the anthropic can take the best parts of the anthropic framework and combine them with GSD. For framework and combine them with GSD. For framework and combine them with GSD. For example, changing the evaluator agent example, changing the evaluator agent example, changing the evaluator agent and combining it with the criteria so and combining it with the criteria so and combining it with the criteria so that the agent knows what the right that the agent knows what the right that the agent knows what the right implementation is.

But if you want to implementation is. But if you want to implementation is. But if you want to use Anthropics framework and set it up use Anthropics framework and set it up use Anthropics framework and set it up on your own, you can implement it by on your own, you can implement it by on your own, you can implement it by creating agents based on their creating agents based on their creating agents based on their respective roles and have them work respective roles and have them work respective roles and have them work together using agent teams. You can use together using agent teams.

You can use together using agent teams. You can use one agent team member as a generator and one agent team member as a generator and one agent team member as a generator and another as an evaluator. The reason for another as an evaluator. The reason for another as an evaluator.

The reason for using agent teams is because they can using agent teams is because they can using agent teams is because they can communicate with each other while sub communicate with each other while sub communicate with each other while sub agents cannot and would have to write to agents cannot and would have to write to agents cannot and would have to write to a document creating overhead. Therefore, a document creating overhead. Therefore, a document creating overhead. Therefore, Claude creates the tasks from the highle Claude creates the tasks from the highle Claude creates the tasks from the highle plan and creates both agents at the same plan and creates both agents at the same plan and creates both agents at the same time where one is implementing while the time where one is implementing while the time where one is implementing while the other is running tests using the other is running tests using the other is running tests using the playright MCP with the browser waiting playright MCP with the browser waiting playright MCP with the browser waiting for updates from the generator so that for updates from the generator so that for updates from the generator so that it can start the testing process.

The it can start the testing process. The it can start the testing process. The evaluator keeps verifying the work and evaluator keeps verifying the work and evaluator keeps verifying the work and communicates the issues with the communicates the issues with the communicates the issues with the generator and they work in coordination generator and they work in coordination generator and they work in coordination to implement the whole app that matches to implement the whole app that matches to implement the whole app that matches your standards. Now all the agents used your standards.

Now all the agents used your standards. Now all the agents used here along with all the resources are here along with all the resources are here along with all the resources are available in AIABS Pro for this video available in AIABS Pro for this video available in AIABS Pro for this video and for all our previous videos from and for all our previous videos from and for all our previous videos from where you can download and use it for where you can download and use it for where you can download and use it for your own projects. If you found value in your own projects. If you found value in your own projects.

If you found value in what we do and want to support the what we do and want to support the what we do and want to support the channel, this is the best way to do it. channel, this is the best way to do it. channel, this is the best way to do it. The links in the description.

That The links in the description. That The links in the description. That brings us to the end of this video. If brings us to the end of this video.

If brings us to the end of this video. If you'd like to support the channel and you'd like to support the channel and you'd like to support the channel and help us keep making videos like this, help us keep making videos like this, help us keep making videos like this, you can do so by using the super thanks you can do so by using the super thanks you can do so by using the super thanks button below. As always, thank you for button below. As always, thank you for button below.

As always, thank you for watching and I'll see you in the next watching and I'll see you in the next watching and I'll see you in the next one.